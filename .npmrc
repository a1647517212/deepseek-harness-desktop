# DSH prerelease packages expose a dense optional peer graph that causes
# npm 11's resolver to consume unbounded time and memory. Runtime completeness
# is enforced by the isolated packaged smoke test in CI.
legacy-peer-deps=true
